#include <stdio.h>

/**
 * main - causes an infinite loop
 * while (i < 10)
 * {
 * putchar(i);
 * }
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n)");

	i = 0;

	printf("Infinite loop avoided :(\n)";

	return (0);
}
